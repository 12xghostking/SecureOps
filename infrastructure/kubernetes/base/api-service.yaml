apiVersion: v1
kind: Service
metadata:
  name: secureops-api
  namespace: secureops
  labels:
    app: secureops-api
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      name: http
      protocol: TCP
  selector:
    app: secureops-api
