apiVersion: v1
kind: Service
metadata:
  name: secureops-web
  namespace: secureops
  labels:
    app: secureops-web
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      name: http
      protocol: TCP
  selector:
    app: secureops-web
